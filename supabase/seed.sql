-- Seed: Create admin user and profile
-- Run this AFTER the migration.sql is applied.
-- This uses Supabase's auth admin functions.

-- Note: The admin user must first be created via Supabase dashboard or the Auth Admin API.
-- Once created, get the user's ID from auth.users and run:

-- INSERT INTO public.profiles (id) VALUES ('<uuid-from-auth.users>');
